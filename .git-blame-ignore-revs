# Revisions to skip when running `git blame`.
#
# Add the repo-wide Prettier reformat commit's full sha below once it is
# committed. That commit touches ~107 files without changing behaviour, so
# leaving it in blame would attribute every line in the repo to the formatter.
#
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub honours this file automatically.
