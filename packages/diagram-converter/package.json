{
  "name": "@swimlane-cloud/diagram-converter",
  "version": "0.1.0",
  "description": "Dependency-free JavaScript converter for the kai-swimlane DSL: parses the DSL and renders SVG/HTML as strings. No React, no DOM — runs in the browser and in Node for server-side rendering.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "files": [
    "src"
  ],
  "exports": {
    ".": "./src/render-pure/index.js",
    "./parser": "./src/parser.js",
    "./themes": "./src/themes.js",
    "./diagram-options": "./src/diagram-options.js"
  },
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
