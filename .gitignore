node_modules/
*.log
.DS_Store

# Build outputs
dist/
dist-electron/
build/
!apps/desktop/build/
out/
# electron-builder output, and nothing else. Named exactly rather than as a
# bare `release/`, which previously swallowed both tools/release and the hub's
# /api/repo/.../release route — a directory called "release" is far more often
# source than build output.
/apps/desktop/release/
.next/
.vercel/
.turbo/

# Env
.env
.env.local
.env*.local

# Misc
.dev-port
coverage/
*.tsbuildinfo
.vercel

# Supabase CLI local state
supabase/.temp/
**/supabase/.temp/
